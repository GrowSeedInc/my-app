{
  "version": "0.2.0",
  "configurations": [
    
    {
      "type": "rdbg",
      "name": "Attach to Rails (Remote)",
      "request": "attach",
      "debugPort": "localhost:12345",
      "cwd": "${workspaceFolder}"
    }
  ]
}
