<% content_for :title, "備品編集" %>

<div class="max-w-2xl">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold text-gray-900">備品編集</h1>
    <%= link_to "詳細に戻る", equipment_path(@equipment), class: "text-gray-600 hover:text-gray-900" %>
  </div>

  <div class="bg-white shadow-sm rounded-lg p-6">
    <%= render "form", equipment: @equipment %>
  </div>
</div>
