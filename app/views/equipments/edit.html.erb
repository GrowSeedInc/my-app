<% content_for :title, "備品編集" %>

<div class="max-w-2xl">
  <div class="flex justify-between items-center mb-4">
    <h1 class="text-xl font-semibold text-gray-800">備品編集</h1>
    <%= link_to "← 詳細に戻る", equipment_path(@equipment), class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>

  <div class="bg-white border border-gray-200 rounded-lg p-6">
    <%= render "form", equipment: @equipment %>
  </div>
</div>
