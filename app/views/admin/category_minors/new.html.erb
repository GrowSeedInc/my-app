<% content_for :title, "小分類を追加" %>

<div class="max-w-lg">
  <div class="flex justify-between items-center mb-4">
    <h1 class="text-xl font-semibold text-gray-800">小分類を追加</h1>
    <%= link_to "← カテゴリー管理に戻る", admin_category_majors_path, class: "text-sm text-gray-500 hover:text-gray-700" %>
  </div>

  <div class="bg-white border border-gray-200 rounded-lg p-6">
    <%= render "form",
          category: @category,
          major_categories: @major_categories,
          medium_categories: @medium_categories,
          selected_major_id: @selected_major_id %>
  </div>
</div>
